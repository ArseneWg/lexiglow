# Contextual lexical item overhaul

This branch unifies exact word-form pronunciation with contextual word meaning.

Temporary implementation note: this file exists only to trigger the normal PR workflows after the one-shot applicator commit, which was authored by github-actions and therefore did not recursively trigger Actions. It will be removed before the PR is finalized.
